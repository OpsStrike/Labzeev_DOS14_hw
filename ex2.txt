#!/bin/bash

arr1=( "foo" "bar" "blah" "test" "123" "foo")
arr2=( "bar" "blah" "test" "1223" "ss" "foo" "dfd")

result=$(comm -12 <(for x in "${arr1[@]}"; do
  echo "${x}";done|sort) <(for x in "${arr2[@]}"; do
    echo "${x}"; done|sort));
echo совпадения: ${result[@]}

